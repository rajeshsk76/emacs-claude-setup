{
  "name": "Emacs + Claude Code",
  "image": "mcr.microsoft.com/devcontainers/universal:2",

  "postCreateCommand": "npm install -g @anthropic-ai/claude-code && mkdir -p ~/.emacs.d && cp .emacs.d/init.el ~/.emacs.d/init.el",

  "waitFor": "postCreateCommand"
}
